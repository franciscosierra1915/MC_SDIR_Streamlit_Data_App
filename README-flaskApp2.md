# Flask Micro-API

This project is a minimal Flask web API created for an academic assignment.

## Endpoints

### GET /hello
Returns a welcome message in JSON format.

### GET /data
Returns a list of sample campus data.

### POST /data
Adds a new data entry using JSON input.

## How to Run

```bash
pip install -r requirements.txt
python app.py
